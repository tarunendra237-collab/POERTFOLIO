services:
  - type: web
    name: portfolio
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true
      - key: GEMINI_API_KEY
        sync: false
      - key: OLLAMA_MODEL
        value: llama2